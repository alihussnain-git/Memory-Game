import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  title: {
    alignSelf: 'center',
    position: 'absolute',
    top: 60,
    fontSize: 22,
    fontWeight: 'bold',
  },
});

export default styles;
